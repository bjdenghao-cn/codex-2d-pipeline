# 2d-pipeline 0.1.0 交付说明

包内 `2d-pipeline/` 是完整 Skill；将此整个目录放入 Codex 用户技能目录，下一轮用 `$2d-pipeline` 调用。启动示例和依赖见包内 README.md。manifest.json 列出源码文件 SHA-256；这不是线上发布或正式美术成片。

## 本次验证

- 34 项测试通过，含真实 Godot 渲染、分阶段生产、审批及局部失效回归；测试使用项目内独立临时目录。
- 合成诊断素材的 15 秒、1080×1920、30 fps、450 帧 H.264/AAC 导出和完整解码已通过。
- 新上下文能按 Skill 分开规划完整剧情镜头与每动作的 6～8 个姿势，补齐非角色资产和按需灰盒。
- 正式 AI 素材、连续剧情观感、接触质量和声音接受仍未验收；未调用付费生图或公开发布。

## 已知非阻断问题

若某个外部步骤改写了上游 JSON 合同后失败，原始状态文件可能暂时保留旧审批显示。再次执行 `status` 或 `run` 会重新读取合同并撤销过期结果，不能据旧状态继续放行。失败后先查询状态，不把手工读取缓存 JSON 当成当前审批。

本版保留这个已披露的小问题；风险是直接读取原始缓存时可能误判状态，不是通过运行入口绕过放行检查。后续版本可改善失败清理时的即时刷新。
