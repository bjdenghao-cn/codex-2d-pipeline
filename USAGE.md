# 小白使用说明

## 1. 先安装 Skill，不用先学写代码

从首页下载版本 ZIP，解压后把整个 `2d-pipeline` 文件夹放入 Codex 用户技能目录。Windows 默认位置为 `C:\Users\你的用户名\.codex\skills\`。保留 scripts、references、assets 等子目录。不要把压缩包本身放进去就当作安装完成。

新建 Codex 任务，输入：

```text
请检查 $2d-pipeline 是否可用，运行它的帮助命令，列出缺少的软件和部署步骤。
暂不生成付费素材，暂不发布。
```

如果找不到 Skill，检查目录是否多套了一层，再重启 Codex。

## 2. 让 Codex 检查并部署依赖

你不需要辨认每条报错。让 Codex 报告“已找到 / 缺少 / 如何安装”，安装后再验证命令真实可用。

| 工具 | 部署与确认 |
| --- | --- |
| Python | 从官方渠道安装；让 Codex 检查解释器版本。本版实测 Python 3.14.6，不代表只支持这个版本 |
| Pillow | 让 Codex 在选定的 Python 环境执行 `python -m pip install -r requirements.txt`，requirements.txt 使用 Skill 内实际路径；需要联网下载时先说明 |
| Godot 4 | 准备官方可执行文件和可用 GPU 桌面图形环境；让 Codex 找到实际路径并跑小测试，不只检查版本号 |
| FFmpeg / ffprobe | 准备官方或可信来源的构建；让 Codex 验证两者可执行路径 |
| 生图工具 | Codex 检查本任务实际开放的生图能力。需要登录由你操作；密钥不要贴到聊天或公开仓库。没有可调用工具，可先提供 PNG，但不会因此凭空生成新姿势 |
| 音频 | 可提供已授权音乐音效；需要配音服务时单独确认。也可明确静音 |

本 Skill **没有内置通用在线生图 API**。换一家生图工具时，Codex 必须适配真实输入输出并实际验证，不能凭空填写插件或接口。

Godot 使用脚本和命令行即可，不强制安装 Godot MCP。Blender、Mixamo、建模与绑骨插件不属于此 2D 流程的必需项。

## 3. 告诉它故事，不要替它写技术方案

复制首页完整启动指令，把故事想法换成你的。先从 15 秒、少角色、少场景开始。明确画幅、风格、是否需要对白，以及可接受的付费预算。

Codex 应先给你完整剧本：发生了什么、遇到什么问题、如何解决、结局是什么。你确认的是故事是否好懂，不是配置格式。

## 4. 每个阶段，你只看这些

| 当前步骤 | Codex 给你看 | 你确认什么 | 下一步 |
| --- | --- | --- | --- |
| 故事与分镜 | 全片故事、各镜头时长 | 起因、转折、结局能看懂吗 | 判断是否需要灰盒 |
| 灰盒（按需） | 用色块代替角色的走位小样 | 谁碰谁、谁挡谁、节奏是否清楚 | 正式素材 |
| 二维素材 | 角色、背景、道具等清单与图 | 人物是否一致，素材是否缺失或侵权 | 动作姿势 |
| 动作 | 每动作通常 6–8 个姿势的连续预览 | 变脸、大小跳变、脚滑、接触是否明显不对 | 全片编排 |
| 完整样片 | Godot 故事预览与声音 | 故事连贯、没有穿帮、声音正确 | 最终导出 |
| MP4 | 可播放成片与检查报告 | 看完听完，确认是否接受 | 保存；发布另行确认 |

灰盒就是“先用方块演一遍”，不是最终画面。姿势数不等于输出帧率：例如 8 个姿势可以通过不同停留时间构成一个动作。实际时长按故事节奏安排。

## 5. 卡住时怎么说

```text
请检查 $2d-pipeline 当前项目状态。
告诉我失败在哪一步、已有成果还能否复用、缺什么，以及下一步最小修复动作。
不要重做已通过的无关步骤，不要自动增加付费重试。
```

更换素材后，下游检查可能需要重新进行，这是为了避免沿用旧审批。外部步骤失败后先让 Codex 运行 `status` 或 `run` 刷新状态，不以缓存文件里的旧结果判断成功。

## 6. 哪些不是成功证明

图片好看不代表动作连贯；能生成 MP4 不代表故事完整；程序测试通过不代表正式美术已通过。当前公开版完成的是本地技术链验证，真实生产的视觉、剧情与声音仍需逐项验收。

首页 AI 配图仅解释流程，不是这套执行器的正式项目成片。请优先查阅 [验证报告](validation.md) 与 [已知问题](DELIVERY.md)。
