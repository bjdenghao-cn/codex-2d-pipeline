# 验证状态

日期：2026-09-07  
版本：0.1.0（首版，不声明稳定或生产就绪）

## 已验证

- Python 3.14.6、Pillow 12.3.0、Godot 4.7.2（真实 OpenGL compatibility/GPU）、FFmpeg/ffprobe n8.1.2 环境中，最终修复后的完整测试为 34 tests、0 failure、0 error、0 skip。使用仓库 scratch 内隔离临时目录；此前系统临时目录两次完整测试出现 Windows 清理错误，测试断言均通过，但那两次执行不计为完整通过。
- TEST_ONLY 合成诊断故事可生成透明姿势序列、Godot 工程、逐帧 PNG、带 AAC 的 H.264/yuv420p MP4，并完成 ffprobe 与完整解码。
- 1080×1920、30 fps、450 帧、15 秒诊断渲染通过；450 张 PNG 均为原生 1080×1920。MP4 为 1080×1920、准确 450 帧、视频与 AAC 均 15.000000 秒。
- 已覆盖合同引用、时间覆盖、变 holds、循环/非循环、alpha/画布/锚点、状态连续、阶段哈希失效、审批与失败停止。机械准备不执行逐姿势自动拟合。
- 新增真实增量阶段验证：未来资产、姿势与声音文件缺失、必要灰盒尚为 pending 时，可以按剧本→灰盒→资产→准备逐步产出。素材变更保留剧本/灰盒审批，姿势变更保留资产审批，声音变更从 render 失效；上游合同或证据变更仍使依赖阶段失效。

这些是合成诊断和技术链证据；`TECHNICAL_ONLY`/`TEST_ONLY` 不可进入 production release。

## 尚未验证

- 实际在线图片生成工具的发现、授权、模型调用和费用；本运行时没有通用在线生成 API。
- 真实 AI 美术的角色/物件一致性、每个真实动作 6–8 姿势、完整场景与 UI 质量。
- 15 秒全分辨率真实剧情的连续动画审美、接触表现、完整声音设计和人工视听接受。
- 更长或更复杂故事的性能，以及当前未实现的骨骼、物理、粒子、非线性 easing、相机旋转、嵌套父级、逐实例透明度和转场。

因此，对真实项目只能在实际生成、观看连续样片并听取混音后分别更新视觉、剧情、声音和审批状态。程序退出 0 或 MP4 可解码不能提升这些状态。

独立 fresh-context WITH-Skill 的规划与启动边界检查已通过：针对 15 秒、450 帧故事，输出了与姿势库存分离的 5 个连续镜头、6 个独立因果事件、13 个动作（每动作 6–8 姿势）、完整非角色资产和接触/跨镜头连续性清单；正确选择 `graybox.required:true` 以先验证双角色共同抬桥的接触与遮挡。变 hold 示例为 8 个姿势、`[3,3,4,4,4,4,3,5]`，共 30 帧，在 30 fps 下为 1 秒。

同一检查真实运行 `init` 成功，并确认未补齐 `screenplay.md` 时 `status` 以 exit 1 fail-closed；没有生成素材、MP4 或伪造审批。该结果只证明 Skill 能指导完整规划、时间语义和正确停止边界，不是正式素材制作、Godot 生产执行或全链路验收。真实视觉、剧情表现和声音仍为 `UNVERIFIED`。

## 部署边界

运行时依赖 Python/Pillow、GPU 可用的 Godot 4、FFmpeg/ffprobe，以及可用且获授权的生图工具或提供的 PNG。没有公司服务、私有入口、个人路径或密钥依赖。安装、ZIP 及原 3D 内容的最终保护检查由发布控制者完成。
